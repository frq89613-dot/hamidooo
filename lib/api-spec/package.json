{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts",
    "codegen:all": "node ../../scripts/run-seq.mjs pnpm run codegen -- pnpm run codegen:check",
    "codegen:check": "pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.9.1"
  }
}
